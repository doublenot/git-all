#!/bin/sh

key=$1
value=$2

for dir in $(find . -d 1); do 
  if [ -d "$dir" ]; then 
    echo ""
    echo "============================"
    echo "Checking status for: $dir"
    cd $dir
    git st
    cd ../
    echo ""
    echo "----------------------------"
    echo ""; 
  fi; 
done;
